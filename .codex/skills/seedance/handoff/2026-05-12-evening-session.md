# 交接会话：路径 C v2 真实 case 验证（2026-05-12 晚间）

> 给晚上接手的另一台 Agent 的交接说明
>
> **使用方法**：用户在另一台电脑打开 CodeBuddy（或别的能读 git repo 的 AI），第一句话只需要说：
> > "读 `handoff/2026-05-12-evening-session.md`，按里面的任务清单跑"
>
> 你（接手 Agent）就按本文件操作即可，不需要重新讲来龙去脉。

---

## 0. 你是谁、要做什么

你是 **Seedance 2.0 Prompt Skill** 项目的临时执行 Agent。这个 skill 的主理人在两台电脑间切换工作——白天那台敲定方法论，晚上这台跑真实 case 验证。

你今晚的任务：**用今天下午刚升级的"路径 C v2 方法论"，跑用户已有的 5 个 case 中最有代表性的 2-3 个，把效果写进 results 文件**，明天主理人在白天那台电脑 `git pull` 直接看你的 case 报告。

---

## 1. 必读背景（3 分钟读完）

### 1.1 项目是什么

`seedance2.0-prompt-skill` 是一个用于**字节跳动即梦平台 Seedance 2.0** 的提示词生成 skill，覆盖三条路径：

- 路径 A：≤15s 概念驱动（用户给文字）
- 路径 B：>15s 长视频流水线
- **路径 C：图片驱动**（用户丢图，今晚的主战场）

入口文件：`SKILL.md`
路径 C 方法论：`references/image-to-prompt.md`
CLI 集成：`references/cli-integration.md`

### 1.2 已具备的 CLI 调用能力

repo 已经集成了 `dreamina` CLI（即梦官方），可以直接命令行跑 image2video。

**关键命令**（你需要会的）：
```bash
dreamina user_credit                                    # 查余额
dreamina image2video -h                                 # 看帮助
dreamina image2video --image="..." --prompt="$(cat prompt.txt)" \
  --model_version=seedance2.0_vip --video_resolution=720p \
  --duration=N --poll=120                               # 提交任务
dreamina query_result --submit_id=xxx                   # 查任务状态
dreamina list_task                                      # 列出历史任务
```

**完整规则**（必读）：`references/cli-integration.md`

### 1.3 今天下午刚做的方法论升级（v2）

主理人昨晚用 5 个 case 试跑，发现 4 个结构性问题，今天下午把它们落进了 `references/image-to-prompt.md`，这 4 条就是你今晚要验证的：

| # | 原则 | 反例 | 解决方案 |
|---|------|------|---------|
| 1 | **画质即风格** | 生活抓拍图被默认渲染成"电影级 CG 高清稳定" → 显假 | 第 1 轮判定"图的来源"，用对应画质关键词；禁止生活类图默认电影级 |
| 2 | **时序角色显式询问** | 默认把图当首帧 → 中间帧场景前后衔接断裂 | 第 1 轮必须问用户"图是首/中/尾/反/闪"；第 2 轮提示词写时间锚点 |
| 3 | **现实物理时间常数** | "她害羞地脸红了"瞬间爆红 → 像化妆 | 现实类反应必须按子节拍展开（脸红 ≥2.5s、哭 ≥3s 等） |
| 4 | **时长服从内容** | 默认打满 15s → 一瞬间反应硬拉 15s 注水 | AI 推算 4/6/8/10/12/15s，主理人有权否决 |

**完整规则**：`references/image-to-prompt.md` 的"第三/四/五/六原则"和"Step 1.4/1.5"。

---

## 2. 今晚的任务清单

### 2.1 Step 0：环境准备

主理人这台电脑没装 dreamina CLI。先检查并安装：

```bash
which dreamina
```

- **如果有输出**（已装）→ 跳到 Step 0.2
- **如果没有**：

```bash
# 询问主理人 dreamina CLI 的安装方式
# 主理人之前在另一台电脑装过，应该有现成的二进制文件或安装脚本
# 如果主理人不记得，可以问他/她之前用的什么方式安装的
```

**Step 0.2：登录**

```bash
dreamina user_credit
```

如果返回 `not logged in` 或类似错误：

```bash
dreamina login
```

按提示完成 OAuth Device Flow（终端会打印 verification_uri、user_code、device_code，主理人在浏览器登录即可）。

**Step 0.3：确认余额**

```bash
dreamina user_credit
```

记录 `total_credit` 数值（白天主理人剩 5077 积分，maestro 档；如果两台电脑共账号，余额一致；否则按当前为准）。

### 2.2 Step 1：选 case

向主理人询问：

> "请告诉我昨晚跑的 5 个 case 各自是什么图 + 什么诉求 + 跑出来什么问题。我们从中挑 2-3 个最能验证 v2 四条原则的重新跑。"

**优先选择标准**：
- 优先 1 个**生活抓拍类图**（验证原则 1：画质即风格）
- 优先 1 个**有明显情绪反应需求的图**（验证原则 3：物理时间常数）
- 优先 1 个**适合作中间帧 / 尾帧的图**（验证原则 2：时序角色）
- 时长可以差异化（验证原则 4：4-15s 弹性）

**如果主理人不愿意逐个讲**：让他/她随机给 2 张图就行，按 v2 流程从头走一遍，目的是观察"按 v2 流程跑出来的视频是否比之前默认电影级 + 默认首帧 + 默认 15s 更真实"。

### 2.3 Step 2：每个 case 的标准流程

对每个选定的 case：

1. **复制 case 模板**：`cp handoff/case-template.md handoff/results/case-XX-<简称>.md`，例如 `case-01-生活抓拍.md`
2. **走完整路径 C v2 流程**（严格按 `references/image-to-prompt.md` 第 1 轮 → 时序询问 → 第 2 轮）：
   - 第 1 轮：读图分析，**必须**输出 `画质来源判定 / 对应画质策略 / 建议时长 / 推算理由 / 平台合规风险` 这些 v2 字段
   - 必须**显式询问主理人时序角色**（首/中/尾/反/闪），不能默认首帧
   - 输出 2-3 个 Hook 候选模式
3. **主理人选定 Hook 后**，输出第 2 轮提示词，**风格总纲必须包含画质来源关键词**，反应必须按子节拍展开，时长 = 推算值
4. **追加 Step 4 CLI 询问**，主理人回 `跑` 才执行
5. **执行**：用 `dreamina image2video` 跑（参数详见 cli-integration.md）
6. **轮询查结果**：每 60-90 秒 `dreamina query_result --submit_id=xxx` 查一次，直到 `gen_status=success` 或 `fail`
7. **下载视频**：成功的话 CLI 会返回 video_url，存到 `handoff/results/videos/case-XX-<简称>.mp4`（下载用 `curl -L "url" -o "path"`）
8. **记录结果**：把整个过程写进 `handoff/results/case-XX-*.md`（用模板字段）

### 2.4 Step 3：硬约束（不许违反）

继承 `cli-integration.md` 的硬约束，重点重申：

1. **主理人没说"跑"就不要跑**——CLI 调用必须用户确认
2. **执行前先 `dreamina user_credit`**——余额 < 100 拒跑
3. **平台合规风险=高 时拒跑**——特别是动漫风暴露内容（侧胸/高开衩等），白天踩过坑
4. **连续失败 3 次必须停下来问主理人**：是提示词问题还是模型问题
5. **每个 case 跑完都要写报告**到 `results/`，不能只在对话里说
6. **提示词必须用临时文件 + `$(cat file)` 读入**（避免中文/引号 shell 转义翻车）

### 2.5 Step 4：交付物清单

今晚结束时，`handoff/results/` 下应该有：

```
handoff/results/
├── case-01-<简称>.md     # 完整记录：图分析 / 时序选择 / 提示词 / submit_id / 出片效果 / 与 v2 原则的对照
├── case-02-<简称>.md
├── (case-03-<简称>.md)
├── videos/
│   ├── case-01-<简称>.mp4
│   └── case-02-<简称>.mp4
└── summary.md            # 当晚总结：v2 原则的 4 条各自有效吗？哪条还要改？
```

**`summary.md` 是最重要的产出**。主理人明天白天读它来决定下一轮要不要再改方法论。

---

## 3. 沟通规范

### 3.1 主理人的偏好

- 直接、不绕弯，不要"很高兴为你服务"开头
- 给选项题（A/B/C），不要开放式问"你想怎么做"
- 失败要如实汇报，不要文过饰非
- 涉及消耗积分的动作必须先问、先报余额

### 3.2 你输出的格式约定

- 每个 case 结束后给主理人一个**简短小结**（5-10 行），格式：
  ```
  ✅ Case-01 [简称] 完成
  - submit_id: xxx
  - 消耗：XX 积分
  - 出片效果：[一句话]
  - v2 原则验证：原则 X ✓ / 原则 Y ⚠️（说明）
  - 视频：handoff/results/videos/case-01-xxx.mp4
  - 完整报告：handoff/results/case-01-xxx.md
  ```
- 跑完所有 case 后追加 `summary.md`，格式见模板第 4 节
- 任何 case 失败，立刻停下问主理人

### 3.3 你不应该做的事

- **不要修改 `references/*.md` 里的方法论**——v2 是白天那台电脑刚定的，今晚是验证而非再改。如果发现哪条不对，记录到 `summary.md` 的"建议改进"段，让主理人决定。
- **不要默默吞失败**——失败必须写进 case 报告
- **不要为了凑 case 数量随便跑**——宁可少跑 1 个，也要每个都按 v2 流程严格走
- **不要修改 `handoff/2026-05-12-evening-session.md`**（这就是本文件）——你是消费者不是生产者

---

## 4. 已知边界 / 踩坑清单

### 4.1 平台审查机制

- **pre-TNS**：提交时审，过得了不代表生成后能过
- **post-TNS**：生成后审，**失败会扣积分但平台会退**（白天验证过），但耗时 5-7 分钟空跑
- **动漫风格不豁免**：动漫暴露 = 真人暴露，post-TNS 一样拦
- **典型失败 reason**：`generation failed: post-TNS check did not pass` → 内容合规问题，换图

### 4.2 image2video 的参数边界

- `--video_resolution=1080p` 仅 `seedance2.0_vip` 支持，其他模型只能 720p
- `--duration` 范围：seedance2.0 family 是 4-15
- `ratio 由输入图自动推断**，不能显式设置`——所以图片比例 = 视频比例
- 异步任务：`--poll=120` 让 CLI 等最多 120 秒，但 seedance2.0 15s 视频通常要 3-5 分钟，主动 `query_result` 轮询

### 4.3 Shell 转义

- 中文 prompt **必须**用临时文件 + `$(cat file)` 读入，不要直接 `--prompt="中文..."`
- 文件名含空格（如 `X 素材`）必须整体加双引号

### 4.4 可能的 CLI 安装坑

- 主理人那台 mac 用的是 `/Users/xiaodingge/.local/bin/dreamina`，确认 PATH 里有 `~/.local/bin`
- 如果晚上这台电脑装不上，可以暂时只做"提示词生成 + 写报告"，跳过 CLI 调用部分（在报告里标注"未执行，仅生成 prompt"）

---

## 5. 第一轮对话建议

你打开本 repo 后，第一条消息建议这样开头：

```
我读完了 handoff/2026-05-12-evening-session.md，今晚的任务清楚了：
按路径 C v2 流程跑 2-3 个 case 验证四条新原则，结果写进 handoff/results/。

请告诉我：
1. 这台电脑装了 dreamina CLI 吗？没装的话你倾向用什么方式装？
2. 昨晚跑的 5 个 case 里，你想优先验证哪 2-3 个？或者直接给我 2 张代表性图片（路径），
   我按 v2 流程从头走。
3. 今晚的预算上限：跑几次？以积分还是数量为准？

我等你这三条信息后开工。
```

---

## 6. 收尾规范

跑完所有 case 后：

1. 写 `handoff/results/summary.md`（用模板第 4 节）
2. 提示主理人：明天白天 `git pull` 即可看完整记录
3. **不要 git commit / git push**（除非主理人明确要求）——主理人控制版本，你只产出文件

---

## 附：四条 v2 原则的验证细则速查

### 原则 1：画质即风格

- ✅ 通过：视频画质明显匹配图来源（生活抓拍 → 手抖、自然光；监控 → 低帧率、噪点；CG → 干净锐利）
- ❌ 失败：仍然是默认电影级，跟图来源不搭

### 原则 2：时序角色

- ✅ 通过：第 1 轮显式问了用户；第 2 轮提示词显式写了时间锚点；视频实际抵达图的时刻接近 K 秒
- ❌ 失败：没问、或者问了但提示词没写锚点、或者视频开头/中段就有图状态导致无铺垫

### 原则 3：物理时间常数

- ✅ 通过：现实类反应明显占够最小时长（脸红能看出来是渐变的、哭是分阶段的）
- ❌ 失败：仍然瞬间完成（0.3 秒爆红、立刻全脸湿）

### 原则 4：时长服从内容

- ✅ 通过：用了非 15s 时长（如 6/8/10s），且视频内容饱满不注水
- ❌ 失败：又默认 15s，或者 5s 视频信息塞太满显得仓促

---

**祝今晚跑得顺利。有问题就问主理人，不要瞎猜。**
